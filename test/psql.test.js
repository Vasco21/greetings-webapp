// const assert = require('assert');
// const helperPool = require('../server/database');
// const helper = require('../helper/helper');

// const databaseConnection = helper();
// databaseConnection.setPool(helperPool);


//  describe(' The Greet with DataBase', async () => {
   
//    it('Should return the name greeted in English', async () => {
//      assert.equal('Hello, Vasco', await databaseConnection.getPool('Vasco', 'english'));
//    });
//     it('Should return the name greeted in Sesotho', async () => {
//       assert.equal('Dumela, Vasco', await databaseConnection.getPool('Vasco', 'sesotho'));
//     });
//     it('Should return the name greeted in Isixhosa', async () => {
//       assert.equal('Molo, Vasco', await databaseConnection.getPool('Vasco', 'isixhosa'));
//     });
//   });